# Phaneris 独立分叉实施方案

日期：2026-09-08，2026-09-14 修订（命名决策改为 Phaneris、补充命名理由、更新核查基线）。核查基线：`b544af79`，根包版本 `0.13.3`。首次成稿基线为 `d6355f11`；两次核查之间上游未进入本地历史，工作分支领先 `origin/main` 298 个提交、落后 0。

状态：实施中。A 阶段（身份冻结）已完成，记录见[Phaneris 分叉 A 阶段记录](phaneris-phase-a-identity-freeze.md)：技术身份已冻结进仓库根的 `phaneris.identity.json` 单一来源，并配有生成器、漂移校验与带理由的残留允许列表。B 阶段（品牌与代码身份）的代码身份、应用身份、数据路径与本地品牌表面已完成并记录于[Phaneris 分叉 B 阶段记录](phaneris-phase-b-brand-identity.md)；服务端点与更新链路仍指向上游，属 D 阶段，因此现在**不可对外发布**。npm 组织名、域名与商标的可用性仍未验证——见 A 阶段记录第 3 节；在有结论前不得对外声明命名空间已归属本项目。未变更远程仓库、应用配置或用户数据。

修订说明：本文件原路径为 `docs/architecture/cizifo-fork-plan.md`，正文曾使用工作名 Sisyphus Agent。经核查（见第 2 节命名理由），两个名称均已放弃，改用 Phaneris。文件同时重命名，全仓无其他文件引用旧路径。

## 1. 目标与完成定义

把现有 fork 建立为独立维护的 Phaneris：品牌独立、操作系统应用身份独立、数据与发布独立。保留来源与 Git 历史，继续按需吸收上游修复。

完成意味着：

- 桌面、Web UI、CLI、服务端、安装包、文档与新产生的分享页面统一使用新身份。
- Phaneris 与上游可同时安装；启动、协议唤起、更新、卸载互不接管。
- 新安装只写入 Phaneris 自有应用数据；旧数据通过显式导入进入新目录，原始数据保持可回退。
- 发布产物和更新元数据来自维护者控制的渠道；产品服务不再隐式依赖上游基础设施。
- 已验证的平台才能列为正式支持；构建成功不等于安装、迁移与更新验证完成。

本次不增加新的 Agent 功能，不重写运行内核，不清除来源历史，不借改名调整数据库业务语义。

## 2. 身份清单

以下标识以 Phaneris（单空格后接可选 Agent）为产品名。**app ID、数据目录与深链接协议必须在首次打包或注册前冻结**：首次发行后再变更，代价从"改字符串"变成"迁移工程"，本机已存在 `~/.craft-agent`（约 8.7 GB 活数据）与已注册的 `craftagents://` 协议即为前置证据。

| 项目 | 目标或建议 | 状态 |
|---|---|---|
| 正式产品名 | Phaneris | 已确定 |
| 完整产品名 | Phaneris Agent | 建议；用于安装包与关于页 |
| 简称 / 短形 | `phan`（CLI 与文档简写） | 建议；不作为正式名 |
| 词源（非产品名） | 希腊语 φανερός（显现、可见） | 参考；不用于软件包名或商标主张 |
| 产品描述 | 本地 AI 工作空间；中断后仍可恢复的持久执行 | 建议 |
| 仓库 | `VanDING/Phaneris` 独立仓库（非 fork）；旧工作仓库 `VanDING/craft-agents-rebuild` 是 fork，保留作历史参考 | 已执行 2026-09-15：GitHub 的 fork 关联无法通过改名解除，因此改为新建非 fork 仓库并推送既有历史，本地 `rebuild` remote 与文档引用同步更新 |
| 根包 / 桌面包 | `phaneris` / `@phaneris/electron` | 建议；私有 workspace 不代表占有公开 npm scope |
| 内部包命名空间 | `@phaneris/*` | 建议；替换现有 `@craft-agent/*`（13 个 workspace 包） |
| CLI | `phaneris` | 建议 |
| app ID / Windows AUMID | `io.github.vanding.phaneris` | 建议；首次发行前冻结 |
| 深链接协议 | `phaneris://` | 建议；不接管 `craftagents://` |
| 产品数据根目录 | `~/.phaneris` | 建议 |
| Electron userData | 在平台标准应用数据位置显式使用稳定目录 `Phaneris` | 建议；与产品数据根分别管理 |
| 环境变量 | `PHANERIS_*` | 建议 |
| 安装产物 | `Phaneris-<version>-<platform>-<arch>.<ext>` | 建议 |
| 预览版 | 独立 app ID、协议、数据与更新缓存，后缀 `preview` | 若提供预览渠道则必须落实 |
| 首个独立版本 | 暂拟 `0.14.0`，开发预览用 `0.14.0-beta.N` | 实施时核对届时版本与标签，禁止版本倒退 |
| 官方域名 / 发布者署名 | 未定；域名未就绪时文档与下载链接指向自己的仓库 | 不编造域名、公司或证书身份 |

不把改名直接等同于 1.0 稳定性承诺。Git remote 暂保留既有语义：`rebuild` 是工作仓库，`origin` 是上游；仓库改名时只更新工作 remote 的 SSH URL，避免同时改变协作习惯。本机工作目录 `E:\craft-agents` 是否随仓库改名一并调整，另行决定；`AGENTS.md` 中关于 `rebuild` 与 `main` 的约定需要在远端改名时同步更新。

2026-09-15 执行记录：按上述做法完成迁移。工作 remote `rebuild` 从 `VanDING/craft-agents-rebuild` 切换到新建的独立仓库 `VanDING/Phaneris`（未使用平台改名，因为改名不会解除 fork 关联），`AGENTS.md`、README、`CONTRIBUTING.md`、`docs/cli.md`、`SECURITY.md`、`Dockerfile.server`、应用内安装指南与 `phaneris.identity.json` 的仓库引用一并更新。旧 fork 未删除，待用户决定归档或删除。

### 2.1 命名理由

**结论：采用 Phaneris。** 它是本轮候选中最干净的一个：纯臆造、显著性最强，且在可检索范围内没有撞上任何活跃软件项目。它对产品叙事也成立——希腊语 φανερός 意为"显现、可见的"，而本产品的核心主张正是"运行可检查、状态可解释、中断后仍不丢失"。

**为什么放弃 Sisyphus：**

1. **同类撞名已经发生，且不是边缘个案。** 检索确认："Sisyphus Agent" 已被 `oh-my-opencode` 用作 agent 名称；另有 `RetricSu/sisyphus`（Crypto AI Agent，其 prompt 直接 `name = "Sisyphus"`）；PyPI 上已有 `sisyphus-ai`；npm 上 `@sisyphus-ai` scope 已被占用。名称、词序与产品类别同时重合，会直接影响搜索区分度，也使该命名空间不可用。
2. **字面语义与产品叙事冲突。** 西西弗斯的经典联想是永恒的徒劳；虽然可以用"巨石滚回来时执行状态仍在"做反转，但这个反转需要解释才能成立，而多数人只会停在字面。
3. **工作名 Workphus 是更差的版本，同样放弃。** `Work` 属描述性成分，削弱商标显著性，也使名字显得平庸；`Workphus` 的发音重音落在区分度最低的 "Work" 上；进入中文语境后故事完全丢失。

**为什么其他候选被淘汰：**

| 候选 | 淘汰原因 |
|---|---|
| Sisyphus / Sisyphus Agent | 与多个活跃项目同类撞名；字面语义为"永恒徒劳" |
| Workphus | `Work` 描述性、显著性与发音均弱，中文语境无故事 |
| cizifo | 仅为仓库旧文件名遗留下来的工作名，从未出现在产品叙事中 |

**Phaneris 的核对情况：**

- 未发现以 Phaneris 命名的软件、产品、公司或 npm 包；最接近的检索命中是 `nomen.at` 的命名表条目（[Phaneris](http://nomen.at/Phaneris)）、化妆品牌 Pharmaceris 与越南企业 Phaner 等无关主体。注意：本轮检索只能证明"没有明显占用"，不构成可用性结论。
- 作为生僻词，陌生感降低撞名概率，同时提高独占性；代价是没有现成文化联想，需要产品自己把意义建立起来（见上）。
- 发音重音固定在第二音节，数字环境中无歧义。
- 首个音节 `pha` 在希腊语系命名中高频出现（Phanes、Pharos、Phasma 等），全部放弃；`phaneris` 是有重音、收尾干脆的词，区别度足够。

**尚未验证的可用性（不得视为已通过）：**

- npm 包名 `phaneris` 与 scope `@phaneris`：**未验证**。本次核查环境到 `registry.npmjs.org` 的 TLS 连接失败，无法查询。
- GitHub 账户/组织下的 `phaneris` 仓库名：**未验证**，同一网络限制。
- 域名与商标：未检查，也未检索商标数据库。
- 以上各项应在首次打包前完成验证，并记录验证日期与方式；结论未出之前，第 2 节的技术标识仍按建议值使用，不得据此对外声明命名空间已归属本项目。

### 2.2 已核查的本机标识冲突

| 标识 | 现状 | 影响 |
|---|---|---|
| `~/.craft-agent` | 已存在，约 8.7 GB（`workspaces` 占 8713 MB），含 `runtime.db` 及 `-wal`/`-shm` 与 `credentials.enc` | 新默认根 `~/.phaneris` 不冲突；旧数据只能通过显式导入进入新根 |
| `HKCU\Software\Classes\craftagents` | 已注册（当前开发构建写入） | 新旧构建会争抢该协议的唤起；`phaneris://` 未占用 |
| `%APPDATA%\Craft Agents` | 已存在（Electron userData） | 新 userData 目录 `Phaneris` 未占用 |
| `~/.phaneris`、`phaneris://` | 未占用 | 迁移窗口仍然开放 |

## 3. 已核查的工程事实

| 文件 / 范围 | 当前事实 | 对方案的影响 |
|---|---|---|
| `apps/electron/electron-builder.yml` | 产品名、app ID、产物名称仍属于上游；当前文件未声明 publish 更新源 | 打包身份和实际生成的更新配置必须一起验收 |
| `apps/electron/src/main/index.ts` | 默认应用名、协议为 Craft；Sentry DSN 来自环境变量 | 修改启动身份；核查构建注入，不能仅据此认定正在向上游发送遥测 |
| `packages/shared/src/config/paths.ts` | 默认 `~/.craft-agent`，支持 `CRAFT_CONFIG_DIR` | 替换为统一新路径策略 |
| `packages/shared/src/credentials/backends/secure-storage.ts` | 独立写死旧凭据路径；默认构造器被凭据管理器使用 | 只修改 CONFIG_DIR 不够，会留下跨应用凭据访问 |
| 同上 | AES-GCM 文件含 `CRAFT01` 标识；密钥派生含 `craft-agent-v2`，保留 v1 读取逻辑 | 这些是格式与密码兼容参数，不能字符串替换 |
| 桌面与 `packages/server/src/index.ts` | 消息网关路径存在旧目录拼接 | 服务端、worker 与桌面必须同时切换 |
| `packages/shared/src/branding.ts` | Viewer 指向 `thecraftagents.com` | 分享上传、查看与删除链路需独立审计 |
| `packages/shared/src/auth/oauth-relay.ts`、`slack-oauth.ts` | OAuth relay / Slack callback 指向上游 | 服务端点、provider 注册与客户端必须同步切换 |
| `scripts/install-app.sh` | 下载来源与 bundle ID 属于上游 | 安装脚本必须重建归属，不能继续下载安装上游 |
| `.github/workflows` | 当前仅发现验证工作流 | 自己的发布工作流仍需建设；尚未审计远端 Secrets 与 Releases |
| `apps/electron/src/main/auto-update.ts` | 第 5 行写明更新来自 `https://thecraftagents.com/electron/latest`，依赖 `electron-updater` | 更新源是代码执行信任根；改名的收益小于重建自有发布链，必须在 D 阶段独立完成 |
| npm scope `@sisyphus-ai` | 已被他人占用（外部事实，非本仓事实） | 印证了放弃 Sisyphus 的判断；`@phaneris` 可用性仍未验证 |

## 4. 品牌独立

### 4.1 产品表面

覆盖 Electron、Web UI、Viewer、marketing、CLI、server 的应用名称与帮助文本，菜单、设置、关于页、窗口标题、系统通知、托盘、错误对话框、OAuth 成功页、Agent 自述与生成文档模板。

替换应用图标、托盘资源、favicon、安装背景和平台图标源。验收小尺寸、深浅背景与 Windows/macOS/Linux 对应产物，不用仅供网页显示的图片替代平台图标资产。

README 保留英文与中文两份；更新贡献指南、问题模板、安全报告入口、主页、支持联系人与新版本说明。历史发行说明和上游贡献链接保留真实来源，不伪装为 Phaneris 原创历史。

### 4.2 单一身份来源

建立产品身份配置，集中声明 displayName、slug、appId、scheme、目录名和受控服务地址。构建脚本、打包配置与运行时代码读取或从中生成配置，避免 YAML、脚本和源码各自维护不同常量。

按用途区分残留项：必须替换的产品身份、必须保留的原作者归属、版本化兼容项、正常第三方服务示例。建立有理由的允许列表；不能用“源码完全没有 Craft”作为完成标准。

保留 LICENSE 与必要归属记录，核对 NOTICE 和 TRADEMARK 的历史适用范围；新产品展示自己的维护者身份，不能把原作者版权直接改成自己的名字。

## 5. 应用身份独立

- 桌面启动与打包使用一致的 app ID、名称和协议；核对 Windows 安装/卸载标识、快捷方式、通知 AUMID，macOS bundle 信息，Linux desktop 文件与 WM class。
- 核查所有打包入口：根脚本、electron-builder、build-win.ps1、build-dmg.sh、资源准备及安装脚本，统一产物约定。
- 显式管理 Electron userData、sessionData、浏览器 profile、更新缓存、日志、锁文件、IPC/socket 命名，防止沿用旧缓存或单实例锁。
- CLI、服务端与 worker 使用统一新身份。服务端口允许配置；不因改名任意改变网络协议，但需要验证并行启动时能报告端口冲突。
- 环境变量新前缀优先。普通变量可临时读旧别名并给出弃用提示；凭据、数据目录、更新源等敏感边界不自动继承旧值。显式指定旧目录用于正常运行时应拒绝或要求进入导入流程。
- `@craft-agent/*` 内部依赖改名使用单独的机械提交，覆盖 workspace 声明、import、TS alias、bundler external、测试 mock 与 lockfile；兼容性协议不跟随包名盲改。

## 6. 数据独立与迁移

### 6.1 路径边界

桌面、CLI、服务端、worker、凭据、日志和资源播种都通过同一套路径解析访问 Phaneris 数据。先收拢散落路径，再启用新默认目录。

区分三类路径：

1. 应用拥有的数据：配置、会话、数据库、附件、Artifact、sources、skills、主题、消息网关状态等，迁入新根。
2. 用户外部项目：代码仓库或工作目录，保持原位置，不因改名移动或递归复制。
3. 旧应用管理的外部工作空间：明确选择复制到新位置，或暂不导入；不能让两个应用同时写同一状态库。

### 6.2 导入流程

首次启动提供“全新开始 / 导入现有数据”，CLI 提供同一引擎的显式导入命令。此处是产品迁移交互，不代表已经获得操作本机真实数据的授权。

流程：检测旧目录及自定义路径 → 生成导入清单 → 确认源进程和 worker 已停止 → 一致性快照 → 写入新目录旁的 staging → 路径映射与凭据处理 → 完整性校验 → 同卷原子提交 → 输出不含秘密的迁移报告。

- 旧目录不删除、不重命名、不写入迁移标记。迁移日志、锁和状态存放在目标侧。
- SQLite 必须使用已停止写入后的完整快照或经验证的备份机制；不能只拷贝主库而漏掉 WAL。
- 使用迁移状态机和来源标识支持重试；已有目标不得覆盖，先提供冲突报告。中断只留下隔离 staging，不把半成品作为正常工作区加载。
- 只转换已知结构字段中的应用路径与深链接；不全局替换聊天正文、附件内容或用户脚本。历史绝对链接使用明确映射解析。
- 核对会话 ID、消息、Artifact revision、附件、sources、模型配置、主题、skills、任务和运行记录。文件校验与数据库引用完整性分别检查。
- 自动化、消息网关、后台任务导入后默认暂停，避免旧应用与新应用重复执行、重复发消息；由用户恢复。未知副作用状态保留，不自动重试。
- 已在执行的任务不热迁移。关闭/停止旧任务后导入；旧浏览器登录态和系统加密数据不承诺可移植，必要时重新登录。

### 6.3 凭据

先保留读取旧格式所需的 magic 和派生参数作为兼容实现。通过专用只读导入路径解密旧副本，在目标以新格式版本和新派生命名写入，再验证可读取；普通运行不回退访问旧凭据路径。

不要调用可能自动迁移、修复或重命名源文件的旧加载器直接读取原件。无法解密时明确标记连接需重新授权，不初始化空凭据库来冒充导入成功；其余可迁移内容可保留并报告局部失败。

当前凭据依赖机器身份，同机导入与跨机器搬迁分开验收。新派生名称用于格式隔离，不宣称能够防御同一系统账户中的其他进程。Windows ACL 与 Unix 文件权限分别验证。

### 6.4 回退

导入后两个数据集独立演化。回退是关闭 Phaneris 后重新使用原应用及其原数据，不是将新数据反向覆盖旧目录。Phaneris 产生的新内容如需返回旧版，需要另行兼容导出。

## 7. 服务和发布独立

### 7.1 外部依赖清单

| 服务 | 目标行为 | 未就绪时 |
|---|---|---|
| 安装与更新 | 自己的 Release、二进制、元数据和更新缓存 | 显示明确不可用状态，禁止回退上游 |
| 文档与反馈 | 自己的仓库/文档与 issue 入口 | 可以直接使用仓库页面 |
| OAuth relay | 自有部署或各 provider 验证可用的本地回调；同步调整注册信息 | 仅禁用受影响授权方式并解释原因 |
| 分享 Viewer | 自有上传、访问、删除和存储规则 | 禁用新建云端分享，保留本地导出 |
| 遥测 | 默认关闭；如启用，仅使用自有项目与明确配置 | 不注入上游 DSN 或 token |
| 第三方集成 | 保留用户主动配置的官方服务 | 不能按域名把合法 Craft 集成误删 |

逐项核对 OAuth client ID 的来源、适用范围及 provider 要求；不假设所有第三方 client ID 都属于上游，也不批量替换。旧分享链接继续作为历史外链保存，不承诺迁移上游托管内容。

### 7.2 发布路径

建议第一阶段以自有 GitHub 仓库 Releases 承载发布；具体 updater provider、平台 target 与元数据格式在实施时按所用版本官方文档确认。正式域名可后接入，不能阻塞应用身份与本地数据独立。

建设 release workflow：校验 tag/包版本 → 锁定依赖及资源版本构建 → 产物身份检查 → 平台签名/公证（按支持平台落实）→ 生成摘要与更新元数据 → 上传 draft release → 安装和更新验收 → 发布。

- Release 元数据只引用同一版本已验证产物；失败时不提升 latest，不发布半套平台清单。
- 签名证书与发布 token 使用自己的 Secrets，PR 验证无权取得；签名后再生成相应校验数据。
- stable 与 preview 不交叉更新；平台/架构匹配，版本单调递增，禁止引用上游包和旧更新缓存。
- 核查 updater 中“找到缓存安装文件即 ready”的回退行为；品牌隔离之外，还需与可信版本元数据和摘要匹配。
- 用两个 Phaneris 测试版本实际完成检查、下载、校验、退出安装、重启和数据保留验证。只修改 feed URL 不算更新链路完成。
- 应急回退优先发布更高版本的修复包；不要通过强制降版本回退不兼容的数据结构。
- 未取得签名或平台验收条件时明确标注测试包，不能称为正式发布已完成。

## 8. 顺序、交付物与验收

默认单任务顺序推进，不开启并行 agent。以下是同一个分叉目标中的检查点，均需完成才能宣告正式独立发布。

| 阶段 | 工作 | 交付物 / 放行条件 |
|---|---|---|
| A 身份冻结 | 确认技术身份、清点引用与外部服务、检查本机标识冲突、验证命名空间（npm 包名与 scope、仓库名、域名/商标），并冻结 app ID、协议与数据目录 | 身份配置规范、可用性验证记录、残留允许列表、外部依赖清单 |
| B 品牌与代码身份 | 图标文案、包/命令、打包身份与路径统一 | 可构建的新品牌包；新安装无旧数据写入；与上游可并存 |
| C 数据导入 | 迁移引擎、凭据兼容、暂停任务、报告与回退 | 合成样本和脱敏副本测试通过；原数据未变 |
| D 服务与发行 | 受控地址、OAuth/分享处理、release workflow 与签名 | 自有 draft release；未就绪服务明确关闭且无上游回退 |
| E 端到端验收 | 安装、导入、协议、worker、更新、卸载 | 验收矩阵有证据；仅声明已测平台支持 |
| F 首次发布 | 中英文说明、来源、迁移指南、已知限制 | 经确认后正式发布并更新仓库身份 |

建议独立提交顺序：身份配置 → 内部包机械改名 → 界面资源 → 应用身份和路径 → 导入引擎 → 服务配置 → 发布流水线 → 文档与验收。避免一个巨大的替换提交掩盖迁移语义。

最低验收矩阵：

| 场景 | 必须验证 |
|---|---|
| 全新安装 | 名称/图标/协议正确，只创建新数据和缓存 |
| 同机并存 | 双方可启动，协议分别唤起，卸载任一方不删另一方数据 |
| 完整导入 | 关键对象数量与引用、附件摘要正确，凭据同机可用 |
| 异常导入 | 中断可重试、目标冲突不覆盖、损坏凭据不清空源、磁盘不足能退出 |
| 数据边界 | 外部项目不移动，不复用上游管理状态，不重复激活自动化 |
| 多入口 | 桌面/CLI/server/worker 使用同一新路径和凭据 |
| 外部服务 | 授权回调、分享和更新仅按配置访问；无上游隐式兜底 |
| 真实升级 | 两个新品牌版本完成升级、重启、数据保留；坏包拒绝安装 |
| 发布产物 | 平台身份、签名状态、元数据、下载地址、版本和摘要一致 |

测试遵循项目约定：先执行迁移、路径、协议、更新配置的聚焦测试；包重命名覆盖多个 workspace，需受影响包的类型检查和生产构建。无需无条件运行全量测试；新增修改或失败再扩大范围。

## 9. 实施前待确定与外部操作边界

相关文档：本方案的上游风险与优先级判断见[项目综合评估与发展路线图](../project-assessment-and-roadmap-2026-08-31.md)第 4.1 节（独立品牌与发布信任链，P0）；公开发布前的签名、校验和、SBOM 与隐私门槛见[发布就绪清单](../release-readiness.md)的"Branding and update chain"一节。

不阻塞规划的建议默认值：Windows 首先验收，其他平台以实际设备/CI条件为准；GitHub Releases 为第一发布渠道；域名未就绪时关闭依赖云端的入口；内部命名在首次独立版本中统一。

首次打包前冻结 app ID、数据目录和协议。正式发布前确定发布者署名、支持平台、签名条件以及 OAuth relay / Viewer 的部署选择。

命名空间与商标验证属于 A 阶段放行条件（见第 2.1 节未验证项）；在有验证结论之前，不得宣称 `phaneris` 相关命名空间已归属本项目。

仓库改名、真实数据迁移、删除/覆盖操作、购买域名与证书、启用外部服务和正式发布分别在结果可审阅时取得所需确认。本次规划不执行这些动作。名称已确定为 Phaneris，不再重复询问是否改名。
